# Arbitrary-Precision-Calculator
Arbitrary-Precision Calculator (APC) is a C-based project designed to perform arithmetic operations (+, -, x, /, %) on integers of arbitrary length. Unlike standard integer types, it uses doubly linked lists to store numbers digit-by-digit, providing accurate results regardless of size — limited only by system memory.
